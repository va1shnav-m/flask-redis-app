services:

  nginx:
    image: nginx

    ports:
      - "8085:80"

    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf

  web:
    build: .

    environment:
      - REDIS_HOST=redis

  redis:
    image: redis

    volumes:
      - redis-data:/data

volumes:
  redis-data:
